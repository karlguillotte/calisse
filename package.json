{
    "name": "calisse",
    "version": "1.0.0",
    "description": "La calisse de big loop",
    "main": "src/index.js",
    "keywords": [],
    "author": "",
    "license": "ISC",
    "scripts": {
        "clean": "rm -rf dist",
        "start": "parcel src/index.html",
        "build": "parcel build src/index.html --public-url /calisse/",
        "deploy": "npm run clean && npm run build && gh-pages -d dist"
    },
    "devDependencies": {
        "gh-pages": "^2.0.1",
        "parcel-bundler": "^1.12.3"
    }
}
